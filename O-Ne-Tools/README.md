# O-Ne Tools

O-Ne 字卡工具入口與編輯器集合。

## 目前可使用
- 一般卡 V1.2.1
- 觸發卡 V1.0.2
- 常駐卡 V1.1.2 READY：任務文字與進度字級由使用者手動調整；只有按下「暫存目前內容」才保留最近 5 次不同內容，並可輸出／載入 JSON 在其他瀏覽器或裝置還原。
- 效果卡 V0.3.1 READY：保留粉紅泡泡既有透明度；薄荷綠暈眩、猩紅陷阱、電光震擊、夢幻棉花糖、金色歡慶以 40% 疊圖透明度呈現，共 6 款 1920×1080 真透明滿版氛圍。
- 移動卡 V1.0.7
- 選項卡 V1.0.1
- 挑戰卡 V0.1.1 READY：沿用固定卡正式「接受／放棄」PSD 外觀；同一工具可切換接受／放棄與 YES／NO 選取，主句三段文字與左右按鈕可修改。
- 對話卡 V1.3.7
- 評分卡 V1.3.1
- 焦點內容卡 V0.5.15：混合中英標題與各列都用 Canvas 實際量字，高度與元件預覽會依換行向下增加；圖片仍可完整顯示、固定 16:9 或自由拉框裁切。
- 縮圖品牌框 V1.2.6
- 說明卡單一編輯 V3：一般說明與純圖片卡在同一編輯器切換，主標／副標／內文預設為 7／3／4 級。
- 片尾結算卡 V0.1.3 READY：QST-03／FORMAL V1.1＋Omi 核准擴充，支援 1–8 列自由增減、Icon 替換、欄位名稱預設／自訂、左側觀眾互動問題／下一影片模式。

## 使用方式
1. 開啟 `index.html`。
2. 在入口頁選擇要製作的字卡。
3. 編輯內容並確認右側預覽；各工具可直接輸出 PNG。
4. 在頂部按「暫存」保留目前內容；從「專案檔案」載入／下載專案、使用批次輸出或查看 AI 格式。PNG 輸出放在預覽下方。

## 共用編輯介面 V1

一般、觸發、常駐、效果、移動、選項、挑戰、對話、評分、焦點內容、縮圖品牌框與片尾結算共 12 個工具，採用說明卡的操作區配置：頂部儲存與專案入口、左側內容設定、右側預覽與底部 PNG 輸出。說明卡維持其原有編輯器。

- 頂部「暫存紀錄」可還原最近 5 次手動暫存；「專案檔案」視窗集中 ZIP、JSON、批次、AI 格式與說明。
- 「內容設定」可暫時收起整個側欄；較長的設定群組可個別展開。具有版型／狀態選單的工具可從上方切換。
- 一般 Canvas 預覽提供「適合畫面」與「100% 原尺寸」；對話卡維持預設 100%。焦點內容卡保留原有元件／影片預覽切換。
- `card-workspace-v1.js` 與 `card-workspace-v1.css` 只整理編輯器介面。各工具的 Canvas renderer、輸出像素尺寸、正式樣式、JSON schema、本機暫存鍵值與原生事件處理維持原有實作。
- `embed=1` 保留既有嵌入介面。載入共用介面失敗時，原始工具仍可操作。

程式驗證：`tests/card-workspace-regression.cjs` 已以 Node.js 24、`jsdom@30.0.1` 與 `@napi-rs/canvas@0.1.100` 執行。依賴可安裝到外部暫存目錄並透過 `NODE_PATH` 載入，正式網頁不需要 npm。執行 `node O-Ne-Tools/tests/card-workspace-regression.cjs` 會比較 12 個工具更新前後的原生控制項、Canvas PNG、編輯、暫存快照、還原與下載事件；基準提交預設為 `07e37a9`，可用 `ONE_WORKSPACE_BASE_REF` 指定。這項測試不取代實際瀏覽器的桌面／窄螢幕畫面檢查。

畫面驗收：根目錄執行 `npm ci`、`npm run dev`，開啟開發伺服器會進入 `tests/workspace-preview.html`。可切換 12 個工具，使用 1920 × 1080、1366 × 900、390 × 844 的 iframe 視窗檢查排版及操作。Vite 與驗收頁僅供開發，正式 GitHub Pages 仍直接使用既有靜態 HTML。

入口頁只讀 `one-tools-registry-v1.json`；`registry.json` 僅保留相容指標，不再複製一份舊工具清單。
Command Center V0.9.4 也會讀取同一份 canonical registry，並顯示全部 13 個可用工具，避免入口數量與導覽內容不同步。依 Omi 明確核准的例外，專案頁直接提供 8 個專案共 47 個母專案、企劃卡與 01～04 Drive／Docs 入口，不再要求每台瀏覽器匯入；Google 權限仍是內容存取的唯一判定。USJ 尚無已核對的唯一企劃卡入口，因此目前提供 5 個，其餘每案 6 個。正式白名單優先於舊 localStorage；本機資料只能補尚未提供的欄位與備份相容，不能覆蓋已核對網址。

## 最近 5 次編輯與載入 JSON

- 目前 13 個可用／候選工具都只有按「暫存目前內容」才新增紀錄；平常輸入、切換選項、還原版本與載入 JSON 都不會暗中儲存。
- 每個工具只保留同一瀏覽器最近 5 次不同內容；相同內容重複按暫存不會新增一筆。
- 未按暫存就重新整理或關閉時，會回到最後一筆已暫存內容；載入 JSON 後也要再按一次「暫存目前內容」才會寫入紀錄。
- 關閉視窗或重新開機不會刪除暫存；清除該網站的瀏覽器資料、無痕模式結束或換裝置則不會保留。
- 共用介面的「暫存紀錄」內提供最近編輯，JSON 載入放在「專案檔案」。從暫存清單選一筆後按「還原」，即可回到該次內容。
- 要跨裝置備份時，可繼續使用工具原有的「輸出／下載設定 JSON」；之後在同一種工具按「載入 JSON」並選擇該檔案。
- 載入檔限制 1 MB，且必須是同一工具的 JSON。格式錯誤、工具不符或欄位無效時會拒絕載入，畫面目前內容不會被覆蓋。
- 評分卡、焦點卡、說明卡、縮圖品牌框與片尾結算卡的單獨 JSON 只保存文字與圖片調整設定，不直接嵌入圖片位元；若要連圖片一起搬移，優先使用「專案檔案」的 ZIP 專案包。說明卡仍保留 `.onecard` 載入／下載，供既有專案相容使用。

共用行為由 `edit-backup-v1.js` V1.3.1 與 `one-tools-ui-v1.js` V1.3.1 提供；各工具仍保留自己的正式 JSON schema 與驗證規則。常駐卡沿用既有本機暫存鍵值，其餘工具也不更換原鍵值，因此升級前的最近紀錄不會被清除。

## 智慧檔名與完整 ZIP 專案包

- PNG、JSON 與專案 ZIP 統一使用「卡片分類-標題-狀態」，不再加入 `O-Ne` 前綴，例如：`焦點卡-道頓堀觀光船-步驟-含字.png`、`常駐卡-大阪城任務-成功.json`。
- 狀態會依目前卡片變體自動判定，例如成功／失敗、可愛、白色／橘色、接受／放棄、含字／空框；同標題的不同版本因此可直接區分。
- 檔名中的 `/ \\ : * ? " < > |` 與控制字元會自動清理；標題空白時使用「未命名」。
- 13 個可用／候選工具都有「完整專案包」區塊，可下載或載入 `.zip`。
- ZIP 會包含：目前預覽 PNG、專案設定 JSON，以及使用者在該工具置入的圖片檔；圖片放在 `assets/` 子資料夾。說明卡的逐步畫面會依項次收齊每張左圖，並在載入時連同各自裁切／縮放／位置設定一起還原。
- ZIP 專案 JSON 使用 `o-ne.project-package.v1`，會記錄 tool id、標題、生成器版本、設定快照與圖片欄位對應；載入時會拒絕其他工具的專案包。
- 圖片回填使用穩定欄位 ID／名稱／語意位置；焦點卡這類動態左右圖片欄位會先還原版面狀態，再把原圖回填到正確側，最後重新套用裁切／縮放設定。
- 專案包目前上限 200 MB，採自包含 ZIP，不依賴外部 CDN；舊 JSON 與最近 5 次暫存機制維持相容。

共用專案包與檔名由 `project-package-v1.js` V1.3.2 提供。使用共用備份的工具由 `edit-backup-v1.js` 自動接入；常駐卡保留既有暫存資料格式。共用編輯介面將既有完成後區塊移入「專案檔案」視窗，不重建原生控制項。

片尾結算卡的欄位與安全框定義另見 `settlement-card-mapping.json`；挑戰卡四個 state 與正式 PSD／PNG 對照見 `challenge-card-mapping.json`。正式 PSD 只作來源，生成器不會覆蓋 Drive 母檔。

## 同卡種批次出圖

- 使用 `batch-render-v1.js` V1.3.1 的工具，每頁只建立一個完成後區塊；共用編輯介面從「專案檔案 → 批次輸出」開啟。
- 同一次批次只接受目前工具的檔案，最多 20 份、總量 200 MB；可混選 `.json` 與 `.zip`。
- 純文字工具可直接批次讀取原本 JSON；評分卡、焦點卡、說明卡、縮圖品牌框、片尾結算卡等圖片型工具，批次模式要求使用 ZIP 專案包，避免輸出缺少置入圖片的殘缺 PNG。
- ZIP 若含專案包內建 PNG，批次會直接取用該 PNG；沒有 PNG 時才開啟隱藏同頁工作器重新套用設定並渲染，不會覆蓋目前正在編輯的畫面。
- 批次前會逐份驗證卡種與格式，顯示「可輸出／請改 ZIP／格式錯誤／卡種不符」；錯誤檔不會混入輸出。
- 完成後只下載一個 `字卡名稱-批次輸出-YYYYMMDD.zip`，裡面是依「卡片分類-標題-狀態」命名的 PNG；同名檔自動加 `-02`、`-03`。
- 可按「停止」中止後續處理；已完成的 PNG 仍會打包下載。

## 給 AI 的 JSON 格式提示

- 共用 `ai-json-guide-v1.js` V1.3.1 的工具，可從「專案檔案 → AI 格式」開啟「給 AI 的 JSON 格式」，JSON 範例預設折疊。說明卡以完整專案 ZIP 搬移圖片，並維持舊 `.onecard` 載入相容。
- 格式直接依目前各工具真正的 JSON exporter／importer 整理，不另造第二套 schema。
- 每張卡提供「複製完整 AI 指令」「複製 JSON 範例」「下載 JSON 範例」；完整 AI 指令會要求 AI 回傳 UTF-8 `.json` 檔，若介面不能建立附件則只回 raw JSON，不加 Markdown 程式碼框或解說。
- 說明區會列出固定 component/schema、重要 enum、陣列數量與其他必要限制；移動卡等結構型工具也會提示站點／路段等相依規則。
- 評分卡、焦點卡、說明卡、縮圖品牌框與片尾結算卡會明確標示：JSON 不包含使用者置入圖片位元；需要連圖片一起交付時使用 O-Ne 專案 ZIP。
- AI 產生的 JSON 仍必須由對應工具「載入 JSON」驗證；格式錯誤或卡種不符時，不得視為正式可用檔。

## 全工具視覺硬規則
- **主底板／卡體底色固定 80% 透明度（opacity = 0.80）。**
- 只有底板底色套用 80%；**不是整張卡片一起 80%**。
- 外框、狀態標籤、識別色條、圖示、文字、選項列與其他前景元件維持各自正式透明度，不得跟著整體變淡。
- 新增任何字卡工具時，必須先檢查此規則再標示可使用。

## 工具入口整理原則
- 一個功能只留一個入口。
- 移動卡不另拆跨國版，跨國需求用每段實線／虛線處理。
- 選項卡不另拆 2 項版；可自由增減選項，每一項可獨立切換亮／暗，允許同時亮多項。
- 挑戰卡不拆接受／放棄頁；接受／放棄與 YES／NO 選取都在 `challenge-card.html` 內切換。
- 觸發卡 EVENT／DONE!／FAIL 在同一編輯器切換。
- 常駐卡 MISSION／DONE!／FAIL 在同一編輯器切換；任務文字與進度字級由使用者手動控制，超出正式安全寬度時阻擋輸出並提示調整；JSON 可備份並載入還原。
- 對話卡已確認為正式卡，後續整合 Omi／涅特／Kuma／Nomi／NPC／雙人與表情選擇。
- 玩家 1 為移動卡同系藍色，玩家 2 為紅色。

## 共用編輯介面瀏覽器驗收

GitHub Actions 的 `Card workspace browser QA` 使用鎖定版本 Chromium／Playwright，執行 12 個工具 × 1920／1366／390 px 共 36 組版面與鍵盤測試，另有 12 組編輯、JSON 往返、暫存還原、專案 ZIP 往返、PNG 與批次 ZIP 實際下載測試。縮圖品牌框另以真實 PNG 驗證專案包圖片回填。固定基準 `07e37a9dfbbddd3e27a91d4a80b6d40c5a12bb53` 用於瀏覽器 Canvas 像素比對；此基準不可默默更新為候選版。

流程僅有 `contents: read`，不含發布權限或密鑰，保留 30 天截圖、下載樣本、失敗 trace 與 HTML／JSON 報告。測試入口為 `tests/card-workspace.browser.spec.cjs`，隔離的靜態服務只在 runner loopback 提供工具檔案。**工作流程全綠不等於視覺核准**：合併前必須打開 artifact 檢視實際畫面，核對字型、溢出、卡面、控制項與手機捲動，再依既有核准決定合併；失敗或缺少證據時保持未合併。

## 維護規則
- 後續新工具或版本更新，直接更新 `O-Ne-Tools/` 內對應檔案。
- 新增工具時只更新 canonical `one-tools-registry-v1.json`；入口頁會自動讀取。
- 公開 HTML／JS／JSON 原則上只能使用 HERE-01、COL-02、QST-03 等語意代號，不得寫入 Drive／Docs URL 或檔案 ID。唯一例外為 Omi 已核准的 `command-center-project-links-direct-v1.js` 專案快捷白名單；它只可包含已核對的母專案、唯一企劃卡與 01～04 入口，不得包含正文、權限資料、憑證或自動化設定。`tests/public-boundary-regression.cjs` 會鎖定這個窄例外並阻擋其他檔案洩漏。
- 不另建平行工具包。
- `assets/NotoSansTC-O-Ne.woff` 依 SIL Open Font License 1.1 使用；授權文字見 `assets/OFL-NotoSansTC.txt`。
